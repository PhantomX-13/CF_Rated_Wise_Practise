#include <iostream>
using namespace std;

int main()
{
ios_base::sync_with_stdio(false);
cin.tie(NULL);

    int a,b, exact;
    cin >> a;

    exact = 7;
    b = exact - a;
    cout << b << '\n';
    return 0;
}
